from setuptools import setup, find_packages

setup(
    name="tetrix-hospital",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # dependencies will be read from requirements.txt
    ],
    python_requires=">=3.8",
) 